import type { OwnedPayloadIdentity } from '../../facts/representation/index.ts';
/** Exact owned /6 codec only. Validate every field before exposing an emitter. */
export declare function preparePackedBodyIdentity(input: unknown): OwnedPayloadIdentity;
