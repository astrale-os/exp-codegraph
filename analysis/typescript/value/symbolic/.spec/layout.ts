import { defineLayout } from '@astrale-os/codegraph/authoring'

export default defineLayout({ entries: ['index.ts', 'model.ts', 'cache.ts', 'coordinates.ts', 'frequency.ts', 'calls.ts', 'engine.ts', 'facts.ts', 'fragment.ts', 'owner.ts', 'table.ts'], exact: true })
