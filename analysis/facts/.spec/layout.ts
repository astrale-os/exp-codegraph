import { defineLayout } from '@astrale-os/codegraph/authoring'

export default defineLayout({
  entries: ['completeness.ts', 'index.ts', 'model.ts', 'owned-admission.ts', 'representation/', 'types.ts'],
  exact: true,
})
