# Specification resources

This module owns stable, reusable authored-resource DTOs shared by normative compilation and
disposable projections. It does not own a mutable module aggregate, implementation observations,
qualification outcomes, filesystem observations, or presentation lifecycle.
