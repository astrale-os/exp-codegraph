# Resident application analysis

One workspace owns the project-session lineage and caller-selected materializer. A refresh resolves
implementation boundaries as observations, refreshes each affected TypeScript universe, and pins
the exact resulting generation set before returning it. Normative specification identities never
include implementation bindings.
