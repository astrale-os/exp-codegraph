# Application snapshot publication

Publication binds portable specification, qualification, analysis, and statistics identities to one
exact repository inventory. The application identity composes content-addressed child identities and
a bounded statistics digest; it never reconstructs a repository-sized serialization of rich child
payloads. Compilation/inventory disagreement aborts publication, while presentation timing and
change reports remain outside the semantic snapshot identity.
