# Focused application selection

Selection changes work and reporting scope, never normative meaning. Requested owners and optional
dependents are expanded through the authored contract dependency closure; every selected result is
advisory while an unselected full repository refresh remains the CI authority.
